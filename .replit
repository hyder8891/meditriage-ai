run = "pnpm install && pnpm run build && pnpm start"
entrypoint = "server/_core/index.ts"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pnpm install && pnpm run build && pnpm start"]
deploymentTarget = "cloudrun"
ignorePorts = false

[[ports]]
localPort = 3000
externalPort = 80
